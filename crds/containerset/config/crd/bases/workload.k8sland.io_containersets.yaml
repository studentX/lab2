
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: containersets.workload.k8sland.io
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.replicas
    name: Replicas
    type: integer
  - JSONPath: .spec.image
    name: Image
    type: string
  group: workload.k8sland.io
  names:
    kind: ContainerSet
    plural: containersets
    shortNames:
    - css
  scope: Namespaced
  subresources: {}
  validation:
    openAPIV3Schema:
      description: ContainerSet is the Schema for the containersets API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ContainerSetSpec defines the desired state of ContainerSet
          properties:
            image:
              description: Image the name and tag of the desired docker image
              type: string
            replicas:
              description: Replicas specifies the desired number of pods
              format: int32
              maximum: 5
              minimum: 1
              type: integer
          type: object
        status:
          description: ContainerSetStatus defines the observed state of ContainerSet
          properties:
            healthyReplicas:
              description: HealthyReplicas the number of active replicas
              format: int32
              type: integer
          required:
          - healthyReplicas
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
