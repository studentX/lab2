---
# Deployment
apiVersion: apps/v1
kind:       Deployment
metadata:
  name: iconoflix
spec:
  replicas: 1
  selector:
    matchLabels:
      CHANGE_ME!
  template:
    metadata:
      labels:
        CHANGE_ME!
    spec:
      containers:
      - name:  iconoflix
        image: quay.io/imhotepio/iconoflix:mem
        resources:
          CHANGE_ME!
        ports:
        - containerPort: 4000
---
# Service
kind:       Service
apiVersion: v1
metadata:
  name: iconoflix
spec:
  type:  CHANGE_ME!
  selector:
    CHANGE_ME!
  ports:
    - port:     4000
      nodePort: CHANGE_ME!